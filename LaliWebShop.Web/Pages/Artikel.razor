@page "/"

@inherits ArtikelBase



@if (Artikels == null)
{
   <Spinner/>
}

else
{
    <h3 class="mb-5">Artikels</h3>
    var artikelMenge = Artikels.Count();

    @if (artikelMenge > 0)
    {
        @*const int itemsRow = 4;
            var artikelsRow = new List<ArtikelDto>();
            @for(int i =0;i < artikelMenge; i++)
            {
            artikelsRow = Artikels.Skip(i * itemsRow).Take(itemsRow).ToList();
            <div class="row mt-3">
            <DisplayArtikel Artikels="@artikelsRow"></DisplayArtikel>
            </div>
            }*@
      
          
                @foreach(var artikelGroup in GetGroupArtikelByKategorie())
            {
                <h4>@GetKategorieName(artikelGroup)</h4>

            <div class="row mt-3">
                <DisplayArtikel Artikels="@artikelGroup.Take(10)"></DisplayArtikel>
            </div>
            }
            
            <hr class="mb-3" />


    }
}





@code {

}
