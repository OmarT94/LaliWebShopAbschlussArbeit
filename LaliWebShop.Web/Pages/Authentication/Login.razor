@page "/login"

<EditForm Model="SignInRequest" OnValidSubmit="LoginUser">
    <DataAnnotationsValidator/>
    <div class="py-4">
        <div class=" row form-group ">
            <div class="col-6 offset-3 ">
                <div class="card border">
                    <div class="card-body px-lg-5 pt-4">
                        <h3 class="col-12 text-success text-center py-2">
                            <strong>Anmelden</strong>
                        </h3>

                        @if (ShowSignInErrors)
                        {
                            <p class="text-center text-danger">@Errors</p>
                        }


                        <div class="py-2">
                               <InputText @bind-Value="SignInRequest.BenutzerName" id="email" placeholder="Email..." class="form-control mb-2" />
                               <ValidationMessage For="(()=>SignInRequest.BenutzerName)"></ValidationMessage>
                        </div>
                        <div class="py-2">
                            <InputText @bind-Value="SignInRequest.Passwort" type="password" placeholder="Passwort..." id="password" class="form-control mb-2" />
                            <ValidationMessage For="(()=>SignInRequest.Passwort)"></ValidationMessage>
                        </div>
                        <div class="py-2">
                            <button type="submit" class="btn btn-success btn-block"><i class="bi bi-person-bounding-box"></i>Anmelden</button>
                            <a href="/registration" class="btn btn-primary text-white m-2"><i class="bi bi-person-plus-fill"></i> Register as a new user</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</EditForm>


@code {

}
